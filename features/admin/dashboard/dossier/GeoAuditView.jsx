'use client';

// Historical export. The operator audit view is now composed in
// `features/admin/dashboard/dossier/audit-lab/OperatorAuditLabView`. This re-export keeps any
// external imports working during the migration without forcing a rename.
export { default } from '@/features/admin/dashboard/dossier/audit-lab/OperatorAuditLabView';
